import ProjectPage from "./ProjectPage";

const MathProject = () => (
  <ProjectPage
    slug="math-project"
    fallbackTitle="The M.A.T.H Project"
    fallbackImage="/afosi_pad2.jpg"
    fallbackBadge="Education & Environment"
    fallbackBadgeColor="bg-green-500"
  />
);

export default MathProject;

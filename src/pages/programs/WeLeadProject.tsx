import ProjectPage from "./ProjectPage";

const WeLeadProject = () => (
  <ProjectPage
    slug="we-lead"
    fallbackTitle="We Lead"
    fallbackImage="/PROJECT-IMAGES/welead-project.jpg"
    fallbackBadge="Empowerment Program"
    fallbackBadgeColor="bg-purple-500"
  />
);

export default WeLeadProject;
